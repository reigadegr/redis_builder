name: ci-build
on:
  workflow_dispatch:
env:
  CARGO_TERM_COLOR: always
jobs:
  release-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup ndk
        run: |
          ndk_url=$(wget -qO- https://github.com/android/ndk/releases/latest | grep -e 'https://dl.google.com/android/repository/android-ndk-.*-linux.zip' | sed -n 's/.*<a href="\([^"]*\)".*/\1/p')
          wget -O ndk.zip $ndk_url -nv
          mkdir ~/ndk_temp
          unzip ndk.zip -d ~/ndk_temp 2>&1 > /dev/null
          mv ~/ndk_temp/*/* ~/ndk_temp
          cp -f libbinder_ndk.so ~/ndk_temp/toolchains/llvm/prebuilt/linux-x86_64/sysroot/usr/lib/aarch64-linux-android/35/libbinder_ndk.so
      - name: Other deps
        run: |
          sudo apt update
          sudo apt install gcc-multilib llvm clang make cmake
      - name: Build_Android
        run: |
          export ANDROID_NDK_HOME=$(realpath ~/ndk_temp)
          export ANDROID_NDK_ROOT=$ANDROID_NDK_HOME
          git clone --depth 1 https://github.com/redis/redis
          cd redis
          export CC="/root/ndk_temp/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android35-clang"
          export CFLAGS="-D__ANDROID__ -fPIE -fPIC -I/root/ndk_temp/toolchains/llvm/prebuilt/linux-x86_64/sysroot/usr/include"
          export LDFLAGS="-pie -llog -L/root/ndk_temp/toolchains/llvm/prebuilt/linux-x86_64/sysroot/usr/lib"
          make -j8

      - name: Prepare artifacts
        run: |
           cd redis
           mkdir output 
           for i in src/redis*; do
               cp $i output
           done
           rm -rf output/*.*
      - name: Upload release version
        uses: actions/upload-artifact@v4
        with:
          name: redis
          compression-level: 9
          path: redis/output
